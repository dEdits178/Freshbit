# Local Docker Database
# DATABASE_URL=postgresql://user:password@localhost:5432/freshbit?schema=public

# Cloud Database (Supabase/Neon/Railway) - Recommended
# Get this from your cloud provider's dashboard
DATABASE_URL=postgresql://user:password@host:5432/freshbit?schema=public

# JWT Configuration
JWT_SECRET=replace-with-strong-secret-minimum-32-characters
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=5000
NODE_ENV=development
